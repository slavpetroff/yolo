# VBW shellcheck configuration
# Info-level suppressions for patterns used intentionally across scripts

# ls usage is intentional for version sorting (ls | sort -V | tail -1)
disable=SC2012

# sed usage preferred over parameter expansion for complex patterns
disable=SC2001

# Parameter expansion pattern quoting (info level, cosmetic)
disable=SC2295
